import { Component } from 'antigravity-core';

export default Component({
  render() {
    return `
      <div class="p-4">
        <h1 class="text-2xl font-bold mb-4">Smart Analytics</h1>
        <div class="grid grid-cols-2 gap-4">
          <div id="chart-container">Chart Placeholder</div>
          <div id="map-container">Map Placeholder</div>
        </div>
      </div>
    `;
  }
});
